

---

## git stash with a descriptive name

Named stashes are much easier to manage than the default `stash@{0}` naming.

```bash
git stash push -m "wip: auth refactor"
git stash list
# stash@{0}: On main: wip: auth refactor
git stash pop stash@{0}
```
