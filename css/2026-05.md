

---

## CSS nesting

Native CSS nesting is now supported without a preprocessor.

```css
.card {
  padding: 1rem;

  & h2 {
    font-size: 1.25rem;
  }

  &:hover {
    background: var(--hover-bg);
  }
}
```

The `&` is required (unlike Sass). Supported in all modern browsers.
